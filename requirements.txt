# Pest-PVT requirements
#
# Target host: CUDA 12.6 driver, 8x GPU
# Strategy: dùng torch+cu111 wheels (mmdet 2.13.0 stack chỉ có pre-built
# mmcv-full cho cu111 max). CUDA 12.6 driver chạy được cu111 binaries
# nhờ backward compatibility của CUDA driver.
#
# Cài 3 bước (chạy thủ công theo thứ tự):
#
#   pip install -r requirements.txt \
#       --extra-index-url https://download.pytorch.org/whl/cu111
#   pip install mmcv-full==1.3.18 \
#       -f https://download.openmmlab.com/mmcv/dist/cu111/torch1.10.0/index.html
#   pip install mmdet==2.13.0
#
# Lý do mmcv-full + mmdet phải cài riêng:
# - mmcv-full cần --find-links trỏ về index của OpenMMLab (không có trên PyPI)
# - mmdet 2.13.0 phải cài SAU mmcv-full để tránh version resolver kéo nhầm

# PyTorch stack (CUDA 11.1 binaries — chạy trên CUDA 12.6 driver)
torch==1.10.0+cu111
torchvision==0.11.0+cu111
torchaudio==0.10.0

# Numerical / vision deps
numpy>=1.19,<1.24
scipy
opencv-python>=4.5
pillow>=8.0
matplotlib
scikit-learn

# Backbone / model utils
timm==0.4.12
einops

# Logging / experiment tracking
wandb
tensorboard

# Annotation / data
lxml
pycocotools

# Utilities
yapf
addict
prettytable
terminaltables
